#include "main.h"

/**
 * _abs - compute the absolute value of an integer
 * @: the integer to check
 * Return: the absolute value of int
 */

int _abs(void)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
